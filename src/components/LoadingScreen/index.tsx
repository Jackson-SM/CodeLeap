import * as Styled from './styles';

export const LoadingScreen = () => {
  return (
    <Styled.LoadingScreen>
      <Styled.LoadingLetter css={{ fontFamily: 'Manrope' }}>
        CodeLeap
      </Styled.LoadingLetter>
    </Styled.LoadingScreen>
  );
};
